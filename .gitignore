# Secrets — never commit these
.streamlit/secrets.toml

# Setup guide (may contain credentials if edited locally — keep off GitHub)
SUPABASE_SETUP.md

# Local match data (only used as fallback when Supabase is not configured)
matches_data.json

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
